Home Value Estimation web application developed using python’s flask micro-framework. The application takes user input through the webpage UI and uses a Random Forest Regression machine learning model to predict the estimated value of a home based on the dataset it was trained with. 

The dataset used can be found here: https://www.kaggle.com/datasets/yasserh/housing-prices-dataset
